name: Pipeline

on:
  # manual trigger
  workflow_dispatch:
  # scheduled trigger
  schedule:
    - cron: '0 0 * * *'
  # push trigger
  push:
    # on main branch
    paths-ignore:
      - README.md
      - .github/**
      - .gitignore

    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest # github hosted runner VM ubuntu
    steps:
      # - name: clone github repo
      #   run: |
      #    git clone https://github.com/github/my-repo.git
      #    cd my-repo
      - name: checkout steps
        uses: actions/checkout@v7

      - name: echo
        run: echo "Hello, World!" >> output.txt

      - name: list files
        run: ls -la

      - name: read file
        run: cat README.md

      # - name: install python
      #   # run: sudo apt-get update && sudo apt-get install -y python3 python3-pip
      #   uses: actions/setup-python@v4  # reuseable workflow 


  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: deploy
        run: echo "Deploying..."
      - name: read output
        run: cat output.txt